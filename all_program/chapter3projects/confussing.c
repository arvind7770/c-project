// this is so simple program but some times students consfuse in this program
#include <stdio.h>

int main(){
    int a=5;
    // if we use a==11 . but we write if(a=11)
    // it means value of a replace to 11
    if (a=11)
    {
        printf("value of a is 11");
    }
    else
    {
        printf("value of a is 5");
    }
    return 0;
}