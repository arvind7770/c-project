// in this program we will learn use of operator like: + - * / %
#include <stdio.h>

int main(){
    // we know addition, subtraction, multipication, division. so we will learn only divisin and modulus
    int a=6;
    int b=3;
    int c= a/b; // we will use division operator
    printf("value of c is: %d\n",c);
    // now we will usse (%) to get reminder(sheshfhal)
    int d=a%b;
    printf("value of d is: %d",d);
    return 0;
}